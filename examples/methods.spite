struct Rect {
    w: i32,
    h: i32,
}

impl Rect {
    fn area(&self) -> i32 {
        return self.w * self.h;
    }
}

@export
fn main() -> i32 {
    let r = Rect { w: 5, h: 3 };
    let a = r.area();
    print(a);
    return a;
}
